{
  "task": "5.2 — Document commands and remove obsolete orchestration references",
  "status": "GREEN for all six acceptance items. This was a documentation-only task: no script or manifest behavior changed. Every command newly described in the docs was run for real against this checkout (not only read/assumed); the remaining orchestration-removal items (Makefile, CircleCI cache, Docker paths) were already completed in Phases 3-4 and are re-confirmed here rather than redone. Full account, exact commands/output, and every disposition in [evidence.md](../../artifacts/workspaces/task5.2/evidence.md).",
  "branch": "npm-workspaces-migration",
  "environment": {
    "node": "v22.16.0",
    "npm": "10.9.2",
    "platform": "darwin arm64"
  },
  "filesChanged": {
    "modified": [
      "README.md — new \"Development\" section: exact Node/npm setup, root npm ci, automatic/explicit compile, workspace dependency edits, scoped test/watch/start/build commands, Insight's separate install, ./ci.sh Docker test workflow, and the backend-directory-is-no-longer-independently-locked / registry-consumer-install-unaffected statement",
      "CONTRIBUTING.md — step 3 now points to README.md#development for environment setup",
      "packages/bitcore-node/README.md — \"Checkout the repo\" now shows root npm ci with the monorepo-vs-registry-consumer distinction",
      "packages/bitcore-wallet-service/README.md — \"Getting Started\" likewise",
      "packages/bitcore-wallet-service/Installation.md — \"Install BWS from github source\" likewise",
      "packages/insight/README.md — documents Insight's deliberate workspace exclusion, its own lockfile, and both install paths (root insight:install/insight:build, or npm ci --workspaces=false run directly inside the package)"
    ]
  },
  "acceptance": [
    {
      "item": "Instructions state exact Node/npm setup, root npm ci, automatic compilation, explicit recompile, scoped tests, watch/start/build commands, workspace dependency edits, Insight's separate install, and supported Docker test workflows.",
      "status": "GREEN",
      "evidence": "README.md#development covers all of these; ./ci.sh --help re-run for real (see evidence.md) as the documented Docker test workflow entry point."
    },
    {
      "item": "Instructions explain that a backend package directory is no longer an independently locked checkout; registry consumers still install published packages normally.",
      "status": "GREEN",
      "evidence": "Stated directly in README.md#development's opening paragraph and repeated in each of the three per-package doc edits (bitcore-node, bitcore-wallet-service x2)."
    },
    {
      "item": "Bootstrap and package-by-package install instructions are replaced wherever they describe monorepo development. Ordinary consumer npm install @bitpay-labs/... examples remain valid.",
      "status": "GREEN",
      "evidence": "The only monorepo-development install instructions found anywhere in tracked docs (bitcore-node, bitcore-wallet-service README + Installation.md) used bare `npm install`; each now shows root `npm ci` with the consumer-install distinction spelled out. crypto-rpc/README.md and Insight's registry-consumer examples were already correct and are unchanged; CONTRIBUTING.md and Insight's own README had no bootstrap/install prose before this task, so there was nothing Lerna-specific there to replace."
    },
    {
      "item": "Tracked executable/configuration files contain no Lerna invocation, dependency, bootstrap implementation, obsolete lock concatenation, or active stale Docker path. Historical changelog prose may mention Lerna.",
      "status": "GREEN (already satisfied by Phases 3-4; re-confirmed here)",
      "evidence": "`git ls-files | xargs grep -lniE '\\blerna\\b'` (excluding node_modules and .idea/) returns only explanatory comments in bitcore-build/sinon-shim/crypto-rpc-EVM-README files contrasting current behavior with the old Lerna layout — no invocation, dependency, or bootstrap code. `.circleci/config.yml` and every tracked Dockerfile were re-grepped for lerna/bootstrap/combined-package-lock and returned nothing."
    },
    {
      "item": "Legacy Travis, Makefile, benchmark and manual-browser workflows each have an explicit retain/update/retire disposition; nested benchmark locks are not accidentally deleted.",
      "status": "GREEN",
      "evidence": "See evidence.md's \"Legacy Travis / Makefile / benchmark / manual-browser dispositions\" section: Makefile — already retired (Task 3.1), re-confirmed absent. .travis.yml (9 files) — retain, inactive, none reference Lerna/bootstrap. Nested benchmark projects (3) — retain, unchanged, excluded from workspaces, no script references, lock file untouched (confirmed in final git status). Manual-browser test/index.html fixtures (5 files) — retain, pre-existing broken asset paths unrelated to Lerna removal or npm-workspaces hoisting (verified empirically: referenced litecore-build package never existed in this repo; files are unreferenced by any script)."
    },
    {
      "item": "New documentation commands are executed in the disposable acceptance checkout. No unit tests are required for prose edits.",
      "status": "GREEN",
      "evidence": "Every newly-documented command was run for real in this session (npm ci --workspaces=false inside packages/insight; npm run insight:install from root; npm install <dep> --workspace=... --dry-run; npm run compile; ./ci.sh --help — outputs in evidence.md). Root npm ci / npm run node / npm run bws / npm run watch / build:docker describe unchanged scripts already exercised for real very recently on this branch in Task 3.1's own evidence, so were not redundantly re-run; git status --short after all verification commands shows only the intended doc edits, confirming no install/compile run left tracked-file churn. No unit tests were added for these prose-only changes, per this criterion."
    }
  ],
  "notCompleted": [
    "Committing this work: left as plain working-tree changes for the user to review, per this repository's standing instruction not to commit autonomously."
  ]
}
